/*
  Public Supabase browser configuration.

  Add the Project URL and the publishable anon key from:
  Supabase Dashboard → Project Settings → API.

  Never put the Supabase service_role key in this file or in the browser.
  The SQL in supabase/schema.sql protects the database with RLS.
*/
window.KHAQAN_SUPABASE = {
  url: '',
  anonKey: ''
};
